// AIRecipeApp target base configuration. All secrets come from the optional local file only
// (do not put empty SUPABASE_* lines here — on some Xcode versions they can win over the include).
// Copy AppSecrets.local.xcconfig.example → AppSecrets.local.xcconfig

// Optional: Instagram relay gist (override in AppSecrets.local.xcconfig).
BACKEND_CONFIG_GIST_RAW_URL = https:$(SLASH)$(SLASH)gist.githubusercontent.com$(SLASH)coderBYC$(SLASH)3207f8bef4c8f9248d79563696042c53$(SLASH)raw$(SLASH)backend_config.json

#include "AppSecrets.local.xcconfig"
